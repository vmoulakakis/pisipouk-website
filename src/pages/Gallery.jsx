import { AnimatePresence,motion } from 'framer-motion'
import { X } from 'lucide-react'
import { useState } from 'react'
import Page,{ Reveal } from '../components/Page'
import { safePhotos } from '../data'
export default function Gallery(){const [active,setActive]=useState(null);const photos=safePhotos.slice(0,12);return <Page><section className="site-shell section-space pt-12"><Reveal><p className="mono-label">ΧΩΡΟΙ & ΔΡΑΣΤΗΡΙΟΤΗΤΕΣ</p><h1 className="display-title mt-4 max-w-5xl text-earth">Οι πραγματικοί χώροι, με ήρεμη κινηματογραφική ματιά.</h1><p className="mt-5 max-w-2xl text-lg leading-8 text-earth/65">Χρησιμοποιούμε μόνο λήψεις χωρίς αναγνωρίσιμα παιδικά πρόσωπα. Οι εικόνες έχουν ήπια χρωματική επεξεργασία χωρίς αλλαγή του χώρου.</p></Reveal></section><section className="site-shell pb-24"><div className="masonry">{photos.map((photo,i)=><motion.button key={photo.src} onClick={()=>setActive(photo)} className="masonry-item group" initial={{opacity:0,y:20}} whileInView={{opacity:1,y:0}} viewport={{once:true}} transition={{delay:(i%4)*.04}}><img src={photo.src} alt={photo.alt} loading="lazy"/><span className="privacy-sticker" aria-hidden="true">✓ privacy-safe</span></motion.button>)}</div></section><AnimatePresence>{active&&<motion.div className="fixed inset-0 z-[70] grid place-items-center bg-earth/92 p-4" initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}} onClick={()=>setActive(null)} role="dialog" aria-modal="true" aria-label="Μεγέθυνση φωτογραφίας"><button className="absolute right-5 top-5 grid h-12 w-12 place-items-center rounded-full bg-white" onClick={()=>setActive(null)} aria-label="Κλείσιμο"><X/></button><motion.img src={active.src} alt={active.alt} className="max-h-[88vh] max-w-[94vw] rounded-3xl object-contain shadow-2xl" initial={{scale:.97}} animate={{scale:1}} onClick={e=>e.stopPropagation()}/></motion.div>}</AnimatePresence></Page>}
