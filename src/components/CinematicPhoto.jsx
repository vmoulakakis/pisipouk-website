import { motion,useReducedMotion,useScroll,useTransform } from 'framer-motion'
import { useRef } from 'react'
export default function CinematicPhoto({src,alt,className='',priority=false,position='center'}){const ref=useRef(null);const reduce=useReducedMotion();const {scrollYProgress}=useScroll({target:ref,offset:['start end','end start']});const y=useTransform(scrollYProgress,[0,1],reduce?[0,0]:[-14,14]);return <div ref={ref} className={`cinematic-frame ${className}`}><motion.img src={src} alt={alt} loading={priority?'eager':'lazy'} fetchPriority={priority?'high':'auto'} style={{y,objectPosition:position}} className="cinematic-image" animate={reduce?undefined:{scale:[1,1.05,1.015]}} transition={reduce?undefined:{duration:14,repeat:Infinity,repeatType:'mirror',ease:'easeInOut'}}/><div className="cinematic-grade" aria-hidden="true"/></div>}
